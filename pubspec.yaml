name: dreams_ai
description: AI-powered dream interpreter with AMOLED black theme
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_dotenv: ^6.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  
  # Animasyonlar
  lottie: ^3.1.2
  flutter_animate: ^4.5.0
  
  # State Management
  provider: ^6.1.2
  
  # Paylaşım
  share_plus: ^10.0.2
  
  # Utilities
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  crypto: ^3.0.3
  
  # Ücretlendirme
  in_app_purchase: ^3.1.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env
